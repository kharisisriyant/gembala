{
  "name": "gembala",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "pnpm --filter @gembala/web dev",
    "dev:api": "pnpm --filter @gembala/api dev",
    "db:up": "docker compose up -d"
  }
}
